# 🎮 NearSnap - Видеочат для геймеров

**NearSnap** — это современное веб-приложение для видеосвязи, специально разработанное для геймеров. Поддерживает до 6 участников одновременно с WebRTC технологией mesh-соединений.

## ✨ Основные возможности

### 🎥 Видеосвязь
- **WebRTC Mesh соединения** - прямые P2P соединения между всеми участниками
- **Адаптивная видео-сетка** - автоматическое изменение макета (1-6 участников)
- **Режим докладчика** - фокус на активном участнике с миниатюрами
- **Полноэкранный режим** - для стриминга и презентаций
- **Детекция речи** - визуальная индикация говорящего участника
- **Демонстрация экрана** - с плавной заменой видео-потока

### 🎨 Интерфейс
- **4 темы оформления**: Default, Dark, Gaming, Neon
- **Адаптивный дизайн** - оптимизирован для мобильных устройств
- **Стеклянный эффект** - современный UI с backdrop-filter
- **Плавные анимации** - красивые переходы и эффекты

### 🎵 Геймерские фичи
- **Soundboard** - 4 звуковых эффекта (GG, Fail, Level Up, Horn)
- **Генерация звуков** - все звуки создаются через Web Audio API
- **Горячие клавиши** - быстрое управление без мыши
- **Админ-панель** - управление комнатой для хоста

### 💬 Коммуникация
- **Текстовый чат** - с анимированными сообщениями
- **Индикатор печати** - видно когда участник набирает сообщение
- **Система уведомлений** - красивые toast-сообщения
- **Список участников** - с местным временем каждого участника

### 🎨 Пользовательский опыт
- **Zen Mode** - автоматическое скрытие интерфейса при бездействии
- **Визуализатор шума** - видео реагирует на громкие звуки
- **Местное время** - отображение часового пояса участников
- **Адаптивный дизайн** - оптимизирован для мобильных устройств

## 🚀 Быстрый старт

### Локальная установка

#### Установка зависимостей
```bash
npm install
```

#### Запуск сервера
```bash
npm start
```

Откройте браузер и перейдите по адресу: **http://localhost:3000**

### 🐧 Установка на Ubuntu Server

#### Автоматическая установка (рекомендуется)
```bash
# Клонируйте репозиторий
git clone https://github.com/EgorSempai/NearSnap.git
cd NearSnap

# Запустите скрипт установки
chmod +x ubuntu-install.sh
./ubuntu-install.sh
```

#### Ручная установка
Подробные инструкции в файле [UBUNTU_INSTALL.md](UBUNTU_INSTALL.md)

#### Быстрые команды для Ubuntu
```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Установка PM2
sudo npm install -g pm2

# Клонирование и запуск
git clone https://github.com/EgorSempai/NearSnap.git nearsnap
cd nearsnap
npm install
pm2 start ecosystem.config.js --env production

# Настройка автозапуска
pm2 startup
pm2 save
```

### ☁️ Облачное развертывание

#### Heroku
```bash
heroku create your-app-name
git push heroku main
```

#### Vercel
```bash
npm install -g vercel
vercel --prod
```

#### DigitalOcean/AWS/VPS
Используйте готовые скрипты:
- `deploy.sh` - автоматическое развертывание
- `docker-compose.yml` - Docker контейнер
- Подробности в [DEPLOYMENT.md](DEPLOYMENT.md)

## ⌨️ Горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl + M` | Включить/выключить микрофон |
| `Ctrl + V` | Включить/выключить камеру |
| `Ctrl + S` | Начать/остановить демонстрацию экрана |
| `F11` | Войти/выйти из полноэкранного режима |
| `Escape` | Выйти из полноэкранного режима |

## 🔧 Системные требования

### Поддерживаемые браузеры
- **Chrome** 60+ (рекомендуется)
- **Firefox** 60+
- **Safari** 12+
- **Edge** 79+

### Устройства
- **Веб-камера** (опционально)
- **Микрофон** (опционально)
- **Стабильное интернет-соединение**

## 🌐 Развертывание в сети

### ⚠️ Важно: Требования HTTPS

Для работы камеры и микрофона **вне localhost** браузеры требуют **HTTPS соединение**. Это ограничение безопасности, которое нельзя обойти.

### Варианты решения:

#### 1. Самоподписанный сертификат (для локальной сети)
```bash
# Создание самоподписанного сертификата
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes

# Обновите server.js для HTTPS:
const https = require('https');
const fs = require('fs');

const options = {
  key: fs.readFileSync('key.pem'),
  cert: fs.readFileSync('cert.pem')
};

const server = https.createServer(options, app);
```

#### 2. Ngrok (для тестирования)
```bash
# Установка ngrok
npm install -g ngrok

# Запуск туннеля
ngrok http 3000
```

#### 3. Cloudflare Tunnel (бесплатно)
```bash
# Установка cloudflared
# Следуйте инструкциям на https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/

cloudflared tunnel --url http://localhost:3000
```

#### 4. Production развертывание
Для production рекомендуется использовать:
- **Nginx** с Let's Encrypt сертификатом
- **Cloudflare** для SSL терминации
- **AWS CloudFront** или другой CDN

## 📁 Структура проекта

```
nearsap/
├── package.json              # Зависимости и скрипты
├── server.js                 # Node.js сервер с Socket.IO
├── README.md                 # Документация
└── public/                   # Клиентские файлы
    ├── index.html            # HTML с мобильной адаптивностью
    ├── css/
    │   └── style.css         # 4 темы + анимации + адаптивность
    └── js/
        ├── main.js           # Основная логика приложения
        └── utils.js          # Утилиты и генератор звуков
```

## 🎯 Архитектура

### Серверная часть (Node.js)
- **Express.js** - веб-сервер
- **Socket.IO** - WebSocket соединения
- **UUID** - генерация уникальных ID комнат

### Клиентская часть (Vanilla JS)
- **WebRTC** - P2P видеосвязь
- **Web Audio API** - генерация звуков
- **CSS Grid** - адаптивная видео-сетка
- **ES6 Modules** - модульная архитектура

### Классы приложения
- `App` - главный контроллер приложения
- `WebRTCManager` - управление WebRTC соединениями
- `WindowManager` - управление UI и экранами
- `Soundboard` - звуковые эффекты
- `AudioSynth` - генерация звуков через Web Audio API
- `ModalManager` - красивые модальные окна

## 🔒 Безопасность

### Защита от взлома
- **Серверная валидация** - все админ-действия проверяются на сервере
- **Проверка прав хоста** - кик и другие действия доступны только реальному хосту
- **Лимит участников** - максимум 6 человек в комнате
- **Валидация данных** - экранирование HTML и проверка входных данных

### Рекомендации
- Используйте **HTTPS** в production
- Настройте **CORS** для конкретных доменов
- Добавьте **rate limiting** для предотвращения спама
- Рассмотрите использование **JWT токенов** для аутентификации

## 🐛 Устранение неполадок

### Камера/микрофон не работают
1. Проверьте разрешения в браузере
2. Убедитесь, что используется HTTPS (кроме localhost)
3. Проверьте, не заняты ли устройства другими приложениями
4. Попробуйте перезагрузить страницу

### Проблемы с соединением
1. Проверьте интернет-соединение
2. Убедитесь, что порт 3000 не заблокирован
3. Проверьте настройки файрвола
4. Попробуйте другой браузер

### Звук не работает
1. Проверьте громкость системы
2. Убедитесь, что Web Audio API поддерживается
3. Попробуйте взаимодействовать со страницей (клик) перед воспроизведением

## 📝 Лицензия

MIT License - свободное использование для любых целей.

## 🤝 Вклад в проект

Приветствуются любые улучшения! Создавайте Issues и Pull Requests.

### Планы развития
- [x] Индикатор "Печатает..." в чате
- [x] Местное время участников
- [x] Автоматическое скрытие интерфейса (Zen Mode)
- [x] Реакция на громкие звуки (Noise Visualizer)
- [x] Полноэкранный режим для стриминга
- [ ] Запись видеозвонков
- [ ] Виртуальные фоны
- [ ] Интеграция с Discord
- [ ] Мобильное приложение
- [ ] Расширенная модерация

---

**Создано с ❤️ для геймерского сообщества**