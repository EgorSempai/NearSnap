# ⚡ Быстрый запуск NearSap

## 🚀 Локальный запуск (для разработки)

```bash
# 1. Установите зависимости
npm install

# 2. Запустите приложение
npm start

# 3. Откройте браузер
# http://localhost:3000
```

**⚠️ Важно**: Камера и микрофон работают только на `localhost` или `HTTPS`!

---

## 🌐 Публичный доступ с HTTPS (Рекомендуется)

### Вариант 1: Cloudflare Tunnel (Самый простой)

```bash
# 1. Запустите приложение
npm start

# 2. В другом терминале создайте туннель
npm run tunnel
# или
npx cloudflared tunnel --url http://localhost:3000

# 3. Получите публичный HTTPS URL
# Пример: https://random-name.trycloudflare.com
```

### Вариант 2: Ngrok

```bash
# 1. Установите ngrok
npm install -g ngrok

# 2. Запустите приложение
npm start

# 3. В другом терминале
ngrok http 3000

# 4. Используйте HTTPS URL из ngrok
```

---

## 🏭 Production развертывание

### Быстрое развертывание с PM2

```bash
# Windows
deploy.bat production

# Linux/Mac
./deploy.sh production
```

### Ручное развертывание

```bash
# 1. Установите PM2
npm install -g pm2

# 2. Запустите в production
npm run pm2:start

# 3. Настройте автозапуск
pm2 startup
pm2 save

# 4. Мониторинг
npm run pm2:monit
```

---

## 🐳 Docker развертывание

```bash
# Быстрый запуск
npm run docker:compose

# Или пошагово
npm run docker:build
npm run docker:run
```

---

## 🔧 Полезные команды

```bash
# Разработка
npm run dev          # Запуск с автоперезагрузкой
npm run health       # Проверка здоровья приложения

# Production
npm run pm2:logs     # Просмотр логов
npm run pm2:restart  # Перезапуск
npm run pm2:stop     # Остановка

# Docker
docker-compose logs -f    # Логи Docker
docker-compose down       # Остановка Docker
```

---

## 🌍 Варианты хостинга

| Платформа | Сложность | Время | HTTPS | Стоимость |
|-----------|-----------|-------|-------|-----------|
| **Cloudflare Tunnel** | ⭐ | 2 мин | ✅ | Бесплатно |
| **Heroku** | ⭐⭐ | 5 мин | ✅ | $7/мес |
| **DigitalOcean** | ⭐⭐ | 10 мин | ✅ | $5/мес |
| **VPS + Nginx** | ⭐⭐⭐ | 30 мин | ✅ | $5-20/мес |

---

## 🔒 Требования HTTPS

**Критически важно**: Браузеры требуют HTTPS для доступа к камере/микрофону вне localhost.

### ✅ Работает:
- `http://localhost:3000`
- `https://your-domain.com`
- `https://random.trycloudflare.com`

### ❌ Не работает:
- `http://192.168.1.100:3000`
- `http://your-domain.com`

---

## 🆘 Решение проблем

### Камера/микрофон не работают
1. Проверьте HTTPS соединение
2. Разрешите доступ в браузере
3. Проверьте, не заняты ли устройства

### Не могу подключиться к комнате
1. Проверьте интернет соединение
2. Попробуйте обновить страницу
3. Проверьте файрвол/антивирус

### Приложение не запускается
```bash
# Проверьте логи
npm run pm2:logs

# Проверьте здоровье
npm run health

# Перезапустите
npm run pm2:restart
```

---

## 📞 Поддержка

- 📖 Полная документация: [DEPLOYMENT.md](DEPLOYMENT.md)
- 🐳 Docker: [docker-compose.yml](docker-compose.yml)
- ⚙️ PM2: [ecosystem.config.js](ecosystem.config.js)

**Готово!** Теперь вы можете запустить NearSap локально или развернуть на сервере! 🎉