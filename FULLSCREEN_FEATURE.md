# 🖥️ Полноэкранный режим для стрима

## Описание функции

Полноэкранный режим превращает NearSap в профессиональный инструмент для стриминга, презентаций и демонстраций. Видео участников занимает весь экран, а интерфейс автоматически скрывается для максимального погружения.

## 🎯 Основные возможности

### Активация полноэкранного режима
- **Кнопка в заголовке**: Значок ⛶ в переключателе режимов отображения
- **Кнопка в панели управления**: "Полный экран" в основных элементах управления
- **Горячая клавиша**: F11 (стандартная клавиша полного экрана)

### Выход из полноэкранного режима
- **Кнопка выхода**: Красная кнопка ✕ в правом верхнем углу
- **Горячая клавиша**: Escape
- **Автоматический выход**: При выходе из полноэкранного режима браузера

## 🎨 Интерфейс в полном экране

### Автоматическое скрытие элементов
- **Заголовок**: Скрывается через 3 секунды бездействия
- **Панель управления**: Скрывается вместе с заголовком
- **Возврат**: При движении мыши или нажатии клавиш интерфейс возвращается

### Специальные элементы управления
- **Кнопка выхода** (✕): Красная кнопка в правом верхнем углу
- **Переключатель чата** (💬): Синяя кнопка для показа/скрытия чата
- **Уведомления**: Информационные сообщения в центре экрана

### Чат в полноэкранном режиме
- **По умолчанию**: Скрыт для максимального погружения
- **Переключение**: Кнопка 💬 или клик по боковой области
- **Стиль**: Полупрозрачный фон с размытием
- **Позиция**: Справа, не перекрывает основное видео

## 🎮 Использование для стриминга

### Идеально подходит для:
- **Игровые стримы**: Полноэкранное видео игрока
- **Презентации**: Демонстрация экрана без отвлекающих элементов
- **Вебинары**: Фокус на докладчике
- **Онлайн-мероприятия**: Профессиональный вид

### Преимущества для стримеров:
- **Чистый интерфейс**: Никаких лишних элементов
- **Автоскрытие**: Интерфейс не мешает контенту
- **Быстрое управление**: Горячие клавиши для всех функций
- **Гибкость**: Можно показать чат при необходимости

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `F11` | Войти в полноэкранный режим |
| `Escape` | Выйти из полноэкранного режима |
| `Ctrl + M` | Переключить микрофон |
| `Ctrl + V` | Переключить камеру |
| `Ctrl + S` | Демонстрация экрана |

## 🎨 Визуальные эффекты

### Плавные переходы
- **Вход**: Плавное масштабирование до полного экрана
- **Скрытие интерфейса**: Элементы плавно исчезают с анимацией
- **Показ интерфейса**: Мгновенное появление при активности

### Стилизация элементов
- **Фон**: Чистый черный для максимального контраста
- **Элементы управления**: Полупрозрачные с размытием
- **Кнопки**: Яркие цвета для легкого обнаружения
- **Уведомления**: Ненавязчивые сообщения в центре

## 🔧 Техническая реализация

### Браузерный API
```javascript
// Вход в полноэкранный режим
element.requestFullscreen()

// Выход из полноэкранного режима  
document.exitFullscreen()

// Отслеживание изменений
document.addEventListener('fullscreenchange', handler)
```

### Кроссбраузерная совместимость
- **Chrome/Edge**: `requestFullscreen()`, `exitFullscreen()`
- **Firefox**: `mozRequestFullScreen()`, `mozCancelFullScreen()`
- **Safari**: `webkitRequestFullscreen()`, `webkitExitFullscreen()`
- **IE**: `msRequestFullscreen()`, `msExitFullscreen()`

### CSS классы
- `.fullscreen-mode` - основной класс полноэкранного режима
- `.auto-hide` - класс для скрытия интерфейса
- `.visible` - класс для показа чата

## 📱 Адаптивность

### Десктоп
- Полная функциональность
- Все горячие клавиши работают
- Автоскрытие интерфейса

### Мобильные устройства
- Адаптированные размеры кнопок
- Сенсорное управление
- Оптимизированное расположение элементов

## 🎯 Сценарии использования

### 1. Игровой стрим
```
1. Запустите игру
2. Нажмите F11 для полного экрана
3. Интерфейс автоматически скроется
4. Зрители видят только игру и ваше видео
```

### 2. Презентация
```
1. Включите демонстрацию экрана (Ctrl+S)
2. Войдите в полноэкранный режим (F11)
3. Презентация занимает весь экран
4. При необходимости покажите чат (💬)
```

### 3. Вебинар
```
1. Настройте камеру на себя
2. Активируйте полноэкранный режим
3. Участники видят вас во весь экран
4. Управляйте через горячие клавиши
```

## 🚀 Будущие улучшения

### Планируемые функции:
- **Запись в полном экране**: Сохранение видео без интерфейса
- **Кастомные оверлеи**: Добавление логотипов и информации
- **Режим "картинка в картинке"**: Миниатюра для многозадачности
- **Интеграция с OBS**: Прямая трансляция в стриминговые сервисы

Полноэкранный режим делает NearSap профессиональным инструментом для любых видеопрезентаций! 🎉